function setCard () {
	//if no card is selected when card slot is clicked, console.log(cardName) -- if no card is in slot 
	//if if card IS selected when card slot is clicked, check 
}


function selectCard () {
	// wut	
}

function checkSlot() {
	// if card.slot[i] !== null { console.log() } 
}